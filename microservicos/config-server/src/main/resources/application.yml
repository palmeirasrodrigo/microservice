server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/palmeirasrodrigo/microservice-repo
          search-paths: fornecedor, loja, transportador